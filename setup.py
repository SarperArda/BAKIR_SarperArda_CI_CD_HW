from setuptools import setup

setup(
    
    name="sarper-arda-bakir-dictionary",
    author="Sarper Arda BAKIR",  
    version="2.0.0",
    packages=["dictionary"],
    install_requires=[
        "requests>=2.23.0",
    ],
    python_requires=">=3.8",

)
